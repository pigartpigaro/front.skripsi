<template>
  <q-page class="q-pa-sm q-mb-xl">
    <div>
      <app-card :is-header="false">
        <template #content>
          <app-table
            title="Data Jenis Perbekalan"
            :columns="table.columns"
            :column-hide="table.columnHide"
            :items="table.items"
            :meta="table.meta"
            :per-page="table.params.per_page"
            :order-by="table.params.order_by"
            :sort="table.params.sort"
            :loading="table.loading"
            :to-search="table.params.q"
            :ada-edit="false"
            :ada-paginasi="false"
            @goto="table.setPage"
            @set-row="table.setPerPage"
            @refresh="table.refreshTable"
            @find="table.setSearch"
            @set-order="table.setOder"
            @new-data="store.newData"
            @delete="table.deletesData"
          >
            <!-- @edit-data="store.editData" -->
            <!--
            row-image="image"
            @delete-ids="table.deletesData"
            -->
            <template #col-jenisperbekalan>
              <div>Jenis Perbekalan</div>
            </template>
          </app-table>
        </template>
      </app-card>
    </div>
    <!-- dialog -->
    <formDialog v-model="store.isOpen" />
  </q-page>
</template>
<script setup>
import { useMasterJenisPerbekalanForm } from 'src/stores/simrs/master/farmasi/jenisPerbekalan/form'
import { useMasterJenisPerbekalanTable } from 'src/stores/simrs/master/farmasi/jenisPerbekalan/table'
import formDialog from './FormDialog.vue'
const table = useMasterJenisPerbekalanTable()
const store = useMasterJenisPerbekalanForm()
table.getDataTable()
</script>
